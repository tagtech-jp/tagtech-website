import Link from 'next/link'
import { PLANS } from '@/lib/plans'

const values = [
  {
    title: '配信中の頭脳を AI にサポートしてもらう',
    desc: 'Gemini 連携の AI 接客カンペが、コメント返しの最適な言葉をリアルタイムで提案。演出・数字管理も同時進行。',
  },
  {
    title: '離れない仕組みを作る',
    desc: 'リスナー個別の記憶・横断ポイントシステム・パーソナライズ接客で、リスナーがあなたの配信から離れられなくなる。',
  },
  {
    title: '数字を見ながら配信できる',
    desc: '「あと何ポイントでイベント入賞？」「この時間帯は何人くる？」をリアルタイムで計算するイベント勝率シミュレーター。',
  },
  {
    title: '自動で SNS 拡散',
    desc: '配信中の盛り上がりを AI が検知 → 自動切り抜き → TikTok / Shorts に投稿。「宣伝できない」を解決。',
  },
]

const targets = [
  {
    label: 'これから配信を始める人',
    sub: '配信を始めたばかりで、まずは無料で機能を試したい段階',
    planName: 'Free',
    color: 'border-blue-100 bg-blue-50',
  },
  {
    label: '小規模配信者',
    sub: 'これからリスナーを増やしたい',
    planName: 'Standard',
    color: 'border-gray-200 bg-gray-50',
  },
  {
    label: '中規模配信者',
    sub: '収益化済み・さらに伸ばしたい',
    planName: 'Premium',
    color: 'border-teal-200 bg-teal-50',
  },
  {
    label: '配信事務所・Vtuber プロダクション',
    sub: '複数ライバーを一元管理したい',
    planName: 'Enterprise',
    color: 'border-gray-200 bg-gray-50',
  },
]

export default function Home() {
  return (
    <main>
      {/* Hero */}
      <section className="max-w-6xl mx-auto px-4 py-20 text-center">
        <p className="text-sm font-medium text-teal-600 mb-3 tracking-wide uppercase">
          配信者向け AI SaaS
        </p>
        <h1 className="text-4xl md:text-5xl font-bold tracking-tight text-gray-900 mb-6 leading-tight">
          配信中の頭脳を<br className="md:hidden" /> AI にサポートしてもらう
        </h1>
        <p className="text-lg text-gray-600 max-w-xl mx-auto mb-8">
          AI で配信エコシステム全体を最適化し、
          <br />
          配信者が孤独な戦いをしない世界を作る。
        </p>
        <Link
          href="/tagdeck"
          className="inline-block rounded bg-teal-600 px-6 py-3 text-sm font-medium text-white hover:bg-teal-700 transition-colors"
        >
          TagDeck の詳細を見る →
        </Link>
      </section>

      {/* Value props */}
      <section className="bg-gray-50 py-16">
        <div className="max-w-6xl mx-auto px-4">
          <h2 className="text-2xl font-bold text-center text-gray-900 mb-10">4 つの提供価値</h2>
          <div className="grid md:grid-cols-2 gap-6">
            {values.map(({ title, desc }) => (
              <div key={title} className="bg-white rounded-lg border border-gray-200 p-6">
                <h3 className="font-semibold text-gray-900 mb-2">{title}</h3>
                <p className="text-sm text-gray-600 leading-relaxed">{desc}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Targets */}
      <section className="max-w-6xl mx-auto px-4 py-16">
        <h2 className="text-2xl font-bold text-center text-gray-900 mb-10">対象ユーザー</h2>
        <div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {targets.map(({ label, sub, planName, color }) => {
            const plan = PLANS.find((p) => p.name === planName)!
            return (
              <div key={label} className={`rounded-lg border p-6 ${color}`}>
                <p className="font-semibold text-gray-900 mb-1">{label}</p>
                <p className="text-sm text-gray-500 mb-3">{sub}</p>
                <p className="text-xs font-medium text-teal-700">{plan.name} — {plan.price}</p>
              </div>
            )
          })}
        </div>
        <div className="text-center mt-10">
          <Link
            href="/tagdeck"
            className="text-sm text-teal-600 hover:text-teal-700 underline transition-colors"
          >
            機能・プラン詳細を確認する →
          </Link>
        </div>
      </section>

      {/* Products */}
      <section className="bg-gray-50 py-16">
        <div className="max-w-6xl mx-auto px-4">
          <h2 className="text-2xl font-bold text-center text-gray-900 mb-10">プロダクト</h2>
          <div className="grid md:grid-cols-2 gap-6">
            <div className="bg-white rounded-lg border border-gray-200 p-6">
              <div className="flex items-center justify-between mb-3">
                <span className="font-bold text-lg text-gray-900">TagDeck</span>
                <span className="text-xs font-medium bg-teal-50 text-teal-700 px-2 py-1 rounded-full">Beta</span>
              </div>
              <p className="text-sm font-medium text-gray-700 mb-2">配信者向け AI 収益管理ダッシュボード</p>
              <p className="text-sm text-gray-600 leading-relaxed mb-4">
                配信中の AI 接客・リアルタイム収益管理・イベント勝率シミュレーターを一画面に集約。
                配信者が数字を見ながら戦略的に配信できる。
              </p>
              <Link href="/tagdeck" className="text-sm text-teal-600 hover:text-teal-700 underline transition-colors">
                詳細を見る →
              </Link>
            </div>

            <div className="bg-white rounded-lg border border-gray-200 p-6">
              <div className="flex items-center justify-between mb-3">
                <span className="font-bold text-lg text-gray-900">TagOshi</span>
                <span className="text-xs font-medium bg-gray-100 text-gray-500 px-2 py-1 rounded-full">構想中</span>
              </div>
              <p className="text-sm font-medium text-gray-700 mb-2">推し活を支える横断視聴ダッシュボード</p>
              <p className="text-sm text-gray-600 leading-relaxed mb-4">
                複数プラットフォームの推し配信者をひとつの画面でフォロー。
                配信通知・横断視聴履歴で推し活をもっと深く、見逃しをゼロに。
              </p>
              <span className="text-sm text-gray-400">準備中</span>
            </div>
          </div>
        </div>
      </section>
    </main>
  )
}
